/// Constants for metadata keys used in export operations.
///
/// These keys are used when generating frontmatter and serializing
/// metadata in various formats (YAML, JSON, XML).
class MetaDataKeys {
  /// The key for the metadata object in JSON serialization.
  static const String metaData = 'metaData';
}
