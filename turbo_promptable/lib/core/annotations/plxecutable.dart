/// Marks a method whose call in source is a trigger. The PLX trigger watcher
/// cuts the call from its line and submits it; the agent then applies the call
/// to the instance it names.
class Plxecutable {
  const Plxecutable();
}
