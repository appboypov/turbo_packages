name: turbo_promptable
description: Object-Oriented Prompting framework for defining AI agent prompts, roles, workflows, and tools as type-safe Dart objects.
version: 0.6.0
resolution: workspace
repository: https://github.com/appboypov/turbo_packages/tree/main/turbo_promptable
homepage: https://github.com/appboypov/turbo_packages
issue_tracker: https://github.com/appboypov/turbo_packages/issues

topics:
  - prompts
  - agents
  - serialization

environment:
  sdk: ^3.10.4

dependencies:
  meta: ^1.17.0
  turbo_serializable: ^0.6.0
  turbo_response: ^1.1.0
  json_annotation: ^4.11.0

dev_dependencies:
  test: ^1.30.0
  lints: ^6.1.0
  build_runner: ^2.13.1
  json_serializable: ^6.13.0
  coverage: ^1.15.0
