/// Preview mode for the TPlayground widget.
enum TurboWidgetsPreviewMode {
  /// No device frame - shows the widget with simple constraints.
  none,

  /// Device frame mode - wraps the widget in a realistic device frame.
  deviceFrame,
}
