/// Positions where contextual content can appear in a [TContextualButtons].
enum TContextualPosition {
  /// Content appears at the top of the wrapper.
  top,

  /// Content appears at the bottom of the wrapper.
  bottom,

  /// Content appears on the left side of the wrapper.
  left,

  /// Content appears on the right side of the wrapper.
  right,
}
