/// Layout options for [TCategorySection].
enum TCategorySectionLayout {
  /// Horizontal scroll layout with items arranged in columns.
  horizontal,

  /// Grid layout with configurable dimensions.
  grid,
}
