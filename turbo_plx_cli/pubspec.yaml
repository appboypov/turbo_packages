name: turbo_plx_cli
description: A Firestore-like API for local file operations via the plx CLI subprocess.
version: 0.0.1
publish_to: none
resolution: workspace
repository: https://github.com/appboypov/turbo_packages/tree/main/turbo_plx_cli
homepage: https://github.com/appboypov/turbo_packages
issue_tracker: https://github.com/appboypov/turbo_packages/issues

topics:
  - cli
  - file-operations
  - firestore-like
  - dart

environment:
  sdk: ^3.10.4

dependencies:
  turbo_response:
  json_annotation: ^4.9.0

dev_dependencies:
  build_runner: ^2.4.0
  json_serializable: ^6.8.0
  test: ^1.24.0
  lints: ^6.0.0
