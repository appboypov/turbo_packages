import 'package:flutter/widgets.dart';

class TFloating extends StatelessWidget {
  const TFloating({Key? key, required this.child}) : super(key: key);

  final Widget child;

  @override
  Widget build(BuildContext context) {
    return child;
  }
}
