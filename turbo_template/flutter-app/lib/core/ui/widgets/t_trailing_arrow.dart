import 'package:flutter/material.dart';

class TTrailingArrow extends StatelessWidget {
  const TTrailingArrow({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) => const Icon(Icons.arrow_forward_rounded, size: 20);
}
