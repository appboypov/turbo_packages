---
status: done
skill-level: junior
parent-type: change
parent-id: add-melos-monorepo-management
---

# Task: Update .gitignore for Melos files

## End Goal
Update the root `.gitignore` file to exclude Melos-generated files and directories to keep the repository clean.

## Currently
There may be a `.gitignore` file in the root, but it likely doesn't include Melos-specific exclusions. Melos generates cache files, symlinks, and other artifacts that should not be tracked.

## Should
The root `.gitignore` file includes patterns to exclude:
- Melos cache directories
- Melos-generated symlinks
- Melos lock files
- Other Melos artifacts

## Constraints
- Must not break existing `.gitignore` patterns
- Should follow standard Melos `.gitignore` recommendations
- Should exclude common Melos artifacts: `.melos/`, `.dart_tool/`, `pubspec.lock` (root only)

## Acceptance Criteria
- [ ] Root `.gitignore` file exists or is created
- [ ] `.melos/` directory is excluded
- [ ] Root `pubspec.lock` is excluded (if generated)
- [ ] Melos cache files are excluded
- [ ] Existing ignore patterns are preserved
- [ ] File follows standard `.gitignore` format

## Implementation Checklist
- [x] 4.1 Check if root `.gitignore` exists
- [x] 4.2 Read existing `.gitignore` content (if exists)
- [x] 4.3 Add Melos-specific exclusions:
  - `.melos/` (Melos cache directory)
  - `pubspec.lock` (root level only, if generated)
  - `.dart_tool/` (if not already excluded)
- [x] 4.4 Preserve existing ignore patterns
- [x] 4.5 Verify `.gitignore` syntax is valid
- [x] 4.6 Test that Melos-generated files are ignored

## Notes
- Melos creates a `.melos/` directory for caching
- Root `pubspec.lock` may be generated by Melos
- Individual package `.gitignore` files should remain unchanged
- Standard Flutter/Dart ignores (like `.dart_tool/`) may already be present
